// LIST OF Enums
// They cannot be created inside a class.

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm,
}
enum Goal {
  lose_weight,
  gain_weight,
  ai_coach,
  gain_insurance,
  just_tryout_app,
}

enum Gender { male, female, other }

enum DietaryPreference { plant_based, carbo_diet, specialized, traditional }

enum ExercisePreference {
  jogging,
  walking,
  hiking,
  skating,
  biking,
  weightLift,
  cardio,
  yoga,
  other,
}

enum ExerciseType {
  cardio,
  strength_Training,
  stretching,
  balance_Training,
  high_Intensity,
  weight_training,
  bodyweight_exercises,
}
